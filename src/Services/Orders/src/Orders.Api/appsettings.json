{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },
  "Database": {
    // Supported values: "PostgreSQL" (default) | "SqlServer"
    // Override via env var:  Database__Provider=SqlServer
    // When running via Aspire, the AppHost propagates DatabaseProvider automatically.
    "Provider": "PostgreSQL"
  },
  "ConnectionStrings": {
    // PostgreSQL — local Docker / Azure Database for PostgreSQL Flexible Server:
    //   "OrdersDb": "Host=<server>.postgres.database.azure.com;Database=orders_db;Username=<user>;Password=<pwd>;SslMode=Require;"
    // SQL Server — local Docker / Azure SQL Database:
    //   "OrdersDb": "Server=<server>.database.windows.net;Database=orders_db;Authentication=Active Directory Managed Identity;"
    "OrdersDb": "Host=localhost;Database=orders_db;Username=postgres;Password=postgres",
    "RabbitMq": "rabbitmq://guest:guest@localhost"
  }
}
